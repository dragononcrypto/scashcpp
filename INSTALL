Building Scash QT UI on Linux

Just use following command:
qmake "USE_UPNP=-"
Should be executed from the root source directory. 
Some prerequisites are required (TODO: describe).
./scash-qt file should be produced.

Building Scash daemon on Linux

Just use following commands:
cd src
make
./scashd file should be produced.
